%% -*- erlang -*-

{erl_opts, [
            warnings_as_errors,
            warn_export_all
]}.

{port_specs, [
              {"priv/emdb_drv.so", ["c_src/*.c"]}
             ]}.

{port_env, [
    {".*", "CFLAGS", "-O2 -Wall"}
]}.
